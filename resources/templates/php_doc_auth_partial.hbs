{{#if hasAuthMethods ~}}
{{#authMethods ~}}
$config = new {{invokerPackage}}\Configuration('CLIENT_ID', 'CLIENT_SECRET', [
    'country' => '{{x-walmart-country}}',  // Default US if not set{{#if isApiKey}}
    'privateKey' => 'PRIVATE_KEY',
    'consumerId' => 'CONSUMER_ID',{{/if}}
]);
{{/authMethods ~}}
{{/if}}
